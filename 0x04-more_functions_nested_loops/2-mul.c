#include "main.h"
/**
 * mul- multiples 2 integes
 * @a: first integr
 * @b: second integer
 * Return: product of multiplying a and b
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
